import { FeedNavbar, StoriesComponent } from "../components";

const FeedLayoutPage = () => {
    return (
        <main>
            <FeedNavbar />
            <StoriesComponent />
        </main>
    );
};

export default FeedLayoutPage;
